# 前端构建期注入的变量(见 docs/tech-solution/frontend.md §10)。
# 复制为 .env / .env.development / .env.production;敏感配置绝不进前端产物
# (前端只持有用户运行时 token,无任何服务端密钥)。.env* 已被根 .gitignore 忽略。

# API 基址:
#   - 开发期留空 → 请求走 Vite dev 代理(vite.config.ts 把 /api、/ws 转发到后端),
#     避开 CORS 复杂度;
#   - 生产构建时指向后端公网地址,例如 https://api.drama-smith.example。
VITE_API_BASE=
